connectionReferences:
  # PLACEHOLDER: This entry must be replaced with the actual connection reference
  # generated by Copilot Studio after the MCP server is added through the portal.
  #
  # Steps:
  #   1. Open Copilot Studio → this agent → Actions → + Add an action
  #   2. Add the ServiceNow MCP server, authenticate, and save
  #   3. Pull files (VS Code extension → Source Control → Pull)
  #   4. The pulled connectionreferences.mcs.yml will contain the real values below
  #
  # Expected format after setup:
  #   - connectionReferenceLogicalName: msdyn_essservicenowcatalog.shared_<connectorId>.<guid>
  #     connectorId: /providers/Microsoft.PowerApps/apis/shared_<connectorId>
  - connectionReferenceLogicalName: msdyn_essservicenowcatalog.shared_mcpservicenow.REPLACE_WITH_ACTUAL_GUID
    connectorId: /providers/Microsoft.PowerApps/apis/shared_mcpservicenow
