node_modules/
dist/
.local/
.env
.env.*
local.settings.json

# macOS Finder metadata
.DS_Store
**/.DS_Store
.azure/
docs/handover.md

*.log
coverage/

# Local-only folder: scratch scripts, credentials notes, temp files
local/
logs/
debug/

# Private documentation (internal implementation details, deployment notes, test results)
# Move sensitive internal docs here: non-public architecture, deployment runbooks, internal reviews
.private-docs/
.private-notes/

# HTTP Archive files from browser testing (may contain tokens, cookies, auth headers)
*.har

# Personal helper scripts (not part of the published toolchain)
find_urls.ps1

# Scratch / temporary markdown (PR body drafts, ad-hoc notes)
*.tmp.md

# Generated MCP App widget modules (HTML embedded as TS string literals).
# Regenerated on every build by scripts/dev/build-widgets.mjs.
src/ui/widgets/generated/

# Power Platform connector scaffolds (paconn init/download into temp folders).
# Only finalized connector definitions should be committed under an explicit path.
.tmp-connector-*/
.tmp-connector/

# Resolved Agent 365 BYO MCP registration payload (contains tenant-specific
# server URL and Entra client ID). Only the *.template.json is checked in.
scripts/agent365-mcp-registration.json
